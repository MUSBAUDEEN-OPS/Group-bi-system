# dependencies
node_modules/

# data generator output (regenerable from source + seed)
data-generator/output/

# next.js
dashboard/.next/
dashboard/out/

# typescript build info
*.tsbuildinfo

# kpi-lib build output (regenerated via npm's pre<script> hooks)
kpi-lib/dist/

# env files
.env
.env.local
!.env.example

# misc
.DS_Store
*.log
coverage/

# auto-regenerated by `next dev`/`next build` on every run — see project
# README; content is not trustworthy (references files that don't exist in
# the installed `next` package) and shouldn't be committed
dashboard/AGENTS.md
dashboard/CLAUDE.md
